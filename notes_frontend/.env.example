# Base URL of the FastAPI backend, e.g.:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
#
# In preview, set this to the backend preview URL (same origin not required; backend must allow CORS).
NEXT_PUBLIC_API_BASE_URL=
